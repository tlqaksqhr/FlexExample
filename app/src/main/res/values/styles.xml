<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#434343</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="HeartRating" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">32dp</item>
        <item name="android:minWidth">32dp</item>
        <item name="android:progressDrawable">@drawable/rating_bar_bg</item>
        <item name="android:indeterminateDrawable">@drawable/rating_bar_bg</item>
    </style>

    <style name="HeartRatingMini" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">9dp</item>
        <item name="android:minWidth">9dp</item>
        <item name="android:progressDrawable">@drawable/rating_bar_bg_mini</item>
        <item name="android:indeterminateDrawable">@drawable/rating_bar_bg_mini</item>
    </style>
</resources>
